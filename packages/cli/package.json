{
  "name": "roster",
  "version": "0.1.0",
  "description": "AI digital twin roster CLI: view and update twin profiles, current work, completed work and presence on a roster server.",
  "type": "module",
  "bin": {
    "roster": "dist/bin.js"
  },
  "files": [
    "dist",
    "assets",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test \"dist/test/*.test.js\""
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
